user:
  name: pinner
  github: mainotgenshin

language: "python"
python_version: "3.13"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "nano inxi"
  start:
    - "python3 main.py"

env:
  - "ENVIRONMENT=production"
